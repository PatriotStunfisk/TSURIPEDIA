import Link from 'next/link';import {notFound} from 'next/navigation';import {cookingFish,getCookingFish} from '@/lib/cooking-data';
export function generateStaticParams(){return cookingFish.map(x=>({fish:x.slug}))}
export async function generateMetadata({params}:{params:Promise<{fish:string}>}){const {fish}=await params;const f=getCookingFish(fish);return f?{title:`${f.name}の捌き方・料理`,description:`${f.name}の捌き方と定番料理をまとめて紹介。`,alternates:{canonical:`/cooking/${fish}`}}:{} }
export default async function FishCookingPage({params}:{params:Promise<{fish:string}>}){const {fish}=await params;const f=getCookingFish(fish);if(!f)notFound();return <div className="section pageTop"><div className="breadcrumb"><Link href="/">ホーム</Link> / <Link href="/cooking">魚料理</Link> / {f.name}</div><div className="pageHero"><span>FISH KITCHEN</span><h1>{f.name}の捌き方・料理</h1><p>釣った{f.name}を下処理して、おいしく食べるところまで。</p></div><section style={{marginTop:28,padding:26,borderRadius:20,background:'#edf6fb'}}><span style={{fontSize:12,fontWeight:900,color:'#087bc4'}}>PREPARATION</span><h2>🔪 {f.name}の基本の捌き方</h2><div style={{display:'grid',gap:10}}>{f.prep.map((x,i)=><div key={x} style={{display:'grid',gridTemplateColumns:'34px 1fr',gap:10,alignItems:'center',padding:13,borderRadius:12,background:'#fff'}}><b>{i+1}</b><span>{x}</span></div>)}</div></section><section style={{marginTop:28}}><span style={{fontSize:12,fontWeight:900,color:'#087bc4'}}>RECIPES</span><h2>{f.name}の料理</h2><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(220px,1fr))',gap:16}}>{f.recipes.map(r=><Link key={r.slug} href={`/cooking/${f.slug}/${r.slug}`} style={{overflow:'hidden',border:'1px solid #dce6eb',borderRadius:18,background:'#fff',color:'#173449'}}>{r.image&&<img src={r.image} alt={`${f.name}の${r.name}`} style={{width:'100%',height:170,objectFit:'cover',display:'block'}}/>}<div style={{padding:17}}><h3 style={{margin:'0 0 7px'}}>{r.name}</h3><p style={{fontSize:13,lineHeight:1.7}}>{r.summary}</p><b style={{color:'#087bc4'}}>作り方を見る →</b></div></Link>)}</div></section><div style={{marginTop:28}}><Link href={`/fish/${f.slug}`} className="searchBtn">← {f.name}の魚図鑑へ</Link></div></div>}
