import Link from 'next/link';import FishIdentifier from '@/components/FishIdentifier';import {fishCatalog} from '@/lib/fish-registry';import {pageSharing} from '@/lib/page-sharing';
export const metadata=pageSharing('/identify','この魚なに？写真から魚種の候補を調べる','撮影・写真選択から魚の候補と見分け方を確認。断定せず複数候補を魚図鑑へつなぎます。');
export default function Page(){return <div className="section pageTop"><div className="breadcrumb"><Link href="/fish">魚図鑑</Link> / この魚なに？</div><span className="eyebrow">UOLINK IDENTIFY</span><h1>この魚なに？</h1><p>写真を手がかりに魚の候補を調べ、図鑑で特徴を確かめましょう。</p><FishIdentifier fish={fishCatalog.map(f=>({slug:f.slug,name:f.name,danger:!!f.hazard}))}/><section id="privacy"><h2>写真と判別結果について</h2><p>画像解析を実行した場合だけ写真をOpenAIへ送信します。UOLINKは送信画像と判別履歴を保存しません。OpenAI側の保持・利用条件はサービスの設定とポリシーに従います。個人情報を含む写真は送らないでください。</p><p><a href="https://platform.openai.com/docs/guides/your-data" target="_blank" rel="noopener noreferrer">OpenAI APIのデータの取り扱い ↗</a></p><p>幼魚・近縁種・写っていない特徴は判別できない場合があります。毒や食用可否は写真で判断せず、専門家へ確認してください。</p><Link href="/spots">釣り場MAPへ →</Link></section></div>}
