import {spots} from '@/lib/data';export const metadata={title:'釣りスポット'};export default function Page(){return <div className="section pageTop"><div className="pageHero"><span>FISHING SPOTS</span><h1>釣りスポット</h1><p>「何が釣れる？」だけでなく、親子向け・駐車場・トイレなども分かる設計。</p></div><div className="mapMock"><div className="mapGrid"></div>{spots.map((s,i)=><div key={s.name} className={'pin p'+(i+1)}>●<span>{s.name}</span></div>)}</div><div className="spotGrid">{spots.map(s=><article key={s.name}><div className="spotTop"><span>{s.type}</span>{s.kids&&<b>👨‍👩‍👧 親子向け</b>}</div><h2>📍 {s.name}</h2><small>{s.area}</small><p>{s.desc}</p><div className="chips"><span>🐟 {s.fish}</span>{s.parking&&<span>🅿 駐車場</span>}{s.toilet&&<span>🚻 トイレ</span>}</div></article>)}</div></div>}
