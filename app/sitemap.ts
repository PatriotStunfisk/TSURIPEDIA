import type {MetadataRoute} from 'next';import {fish} from '@/lib/data';export default function sitemap():MetadataRoute.Sitemap{const base='https://tsuripedia.jp';return ['','/fish','/methods','/spots','/gear','/game',...fish.map(f=>`/fish/${f.slug}`)].map(url=>({url:base+url,lastModified:new Date(),changeFrequency:url.startsWith('/fish/')?'monthly':'weekly',priority:url===''?1:.8}))}
