# 2026-09-17 実装・検証記録

- 全63魚種の料理を監査。48レシピ追加（生食用に処理管理された海産魚の料理36件、ウナギ・ヤマメ・ワカサギ各4件）。個別一覧は `recipe-audit.json`。
- 魚図鑑は明示指定した代表料理を最大4品表示。全レシピは既存 `/cooking/[fish]` へ集約し、URLを維持。
- ヤマメ・ワカサギの新規プロフィール、ウナギの完成、GUIDE 3本・QUICK GUIDE 1本、ワカサギ釣法を追加。
- MAPは5地点・エリアと1船宿を追加、計79件（休業・閉鎖4件を除く初期表示75件）。全件の主分類・特徴は `spot-classification-audit.json`。
- 分類は主分類と複数特徴を分離。尼崎・鳴尾浜等の管理施設を海釣り施設へ。凡例は複数選択フィルター。全選択・全解除、フィルター後クラスタリング。
- クラスタの画面上セル幅は旧40/30/22/12pxから、zoom<6:32px、<8:20px、<10:10px、それ以降4pxへ。同座標付近は集合を残す。スマホ関西表示から拡大し、尼崎・鳴尾浜の個別表示を確認。
- 60 GLBの埋込テクスチャを軽量化。754,183,052 → 252,203,568 bytes（66.6%削減）。元ファイルは無変更。全ファイルの形状バッファ・スキン・アニメーションの一致をテスト。アカエイ・ミノカサゴの外観、ドラッグ、スマホの3D表示をブラウザ確認。
- 692ページに1200×630 JPEG共有カード。absolute canonical / OG / Twitterを共通化。画像最大93,286 bytes。既存の画像不足や不統一を修正。X側の既存投稿キャッシュは強制更新できない。
- 公開釣果：Supabase REST/Storage、審査待ち、レート制限、削除キー、非公開写真の実装。未接続時は公開受付を停止。端末内IndexedDB保存は利用可能。QUESTの実釣欄と共有し、ゲーム捕獲と分離。
- 写真判別：OpenAI Responses API接続実装、同意、縮小・EXIF除去、登録魚最大3候補、図鑑リンク。未設定時の偽判定なし。

## 実行済み検証

- TypeScript、lint、本番build成功。自動テスト103/103成功。
- ローカルHTTPで共有対象692ページ、内部リンク895件、画像243件を確認。404なし。Amazonリンク633件すべて `uolink-22`。
- 基本10魚種とウナギ・ヤマメ・ワカサギの13ページ表示・料理一覧リンクをブラウザ確認。
- PC MAP選択、全解除、複数タイプ再選択。390pxの同一サイトiframe内でもMAP、横スクロール凡例、詳細カード、GUIDE、レシピ、トップ、判別画面を確認（ブラウザのviewport設定が反映されないため実幅iframeを使用。検証ファイルは公開対象から削除）。
- 端末保存のテスト記録をlocalhostだけへ作成し、QUEST実釣欄・リロード後の保持を確認。本番へ架空釣果は送信していない。
- 写真選択→縮小プレビューを実操作。公開釣果と画像解析は模擬プロバイダー応答のAPIテストまで。外部の実サービス疎通とは区別。

## 外部接続が必要な範囲

本番DB、秘密情報、課金APIの変更は行っていない。`platform-setup.md` に適用SQL、環境変数、審査・有効化手順を記載。公開釣果とAI判別の本番接続はこの設定・承認後に検証が必要。
