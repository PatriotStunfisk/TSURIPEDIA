import type {CookingFish} from '@/lib/fish-species/types';
import ProfileImage from './ProfileImage';
export default function FishPreparation({fish}:{fish:CookingFish}){return <div style={{display:'grid',gap:16}}>{fish.prep.map((text,index)=>{const image=fish.prepImages?.find(x=>x.step===index);return <article key={index} style={{background:'#fff',borderRadius:12,overflow:'hidden'}}>{image&&<figure style={{margin:0}}><ProfileImage optimized src={image.src} alt={image.alt} sizes="(max-width: 700px) 100vw, 700px" style={{display:'block',width:'100%',height:'auto',maxHeight:400,objectFit:'contain'}}/><figcaption style={{padding:'4px 16px',fontSize:11,color:'#637782'}}>工程を説明する生成イメージ</figcaption></figure>}<div style={{padding:16}}><b style={{fontSize:12,color:'#087bc4'}}>STEP {index+1}</b><p style={{margin:'6px 0 0',lineHeight:1.8}}>{text}</p></div></article>})}</div>}
