'use client';
import {useEffect,useRef} from 'react';
import * as THREE from 'three';
export default function FishViewer(){const ref=useRef<HTMLDivElement>(null);useEffect(()=>{const el=ref.current;if(!el)return;const scene=new THREE.Scene();const camera=new THREE.PerspectiveCamera(40,el.clientWidth/el.clientHeight,.1,100);camera.position.set(0,.3,7);const renderer=new THREE.WebGLRenderer({antialias:true,alpha:true});renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.setSize(el.clientWidth,el.clientHeight);el.prepend(renderer.domElement);scene.add(new THREE.AmbientLight(0xc7e9ff,3));const key=new THREE.DirectionalLight(0xffffff,4);key.position.set(4,6,5);scene.add(key);const rim=new THREE.DirectionalLight(0x50cfff,3);rim.position.set(-4,1,-4);scene.add(rim);const group=new THREE.Group();scene.add(group);
const length=5.8,seg=100,rings=24,verts:number[]=[],idx:number[]=[];for(let i=0;i<=seg;i++){const t=i/seg,x=(t-.5)*length;const taper=Math.pow(Math.sin(Math.PI*t),.5);const h=.28*taper*(.94+.06*Math.cos(t*Math.PI));const w=.095*taper;for(let j=0;j<rings;j++){const a=j/rings*Math.PI*2;verts.push(x,Math.cos(a)*h,Math.sin(a)*w)}}for(let i=0;i<seg;i++)for(let j=0;j<rings;j++){const a=i*rings+j,b=i*rings+(j+1)%rings,c=(i+1)*rings+(j+1)%rings,d=(i+1)*rings+j;idx.push(a,b,d,b,c,d)}const geo=new THREE.BufferGeometry();geo.setAttribute('position',new THREE.Float32BufferAttribute(verts,3));geo.setIndex(idx);geo.computeVertexNormals();const silver=new THREE.MeshPhysicalMaterial({color:0xc7d6df,metalness:.92,roughness:.2,clearcoat:.45});group.add(new THREE.Mesh(geo,silver));const head=new THREE.Mesh(new THREE.ConeGeometry(.31,.62,4),silver);head.rotation.z=Math.PI/2;head.scale.z=.45;head.position.x=-length/2-.15;group.add(head);const eye=new THREE.Mesh(new THREE.SphereGeometry(.09,24,24),new THREE.MeshStandardMaterial({color:0x05080b}));eye.position.set(-length/2-.28,.11,.105);group.add(eye);const iris=new THREE.Mesh(new THREE.SphereGeometry(.035,16,16),new THREE.MeshStandardMaterial({color:0xe6f6ff,emissive:0x315a70}));iris.position.set(-length/2-.31,.13,.16);group.add(iris);const finMat=new THREE.MeshStandardMaterial({color:0x8aa9bb,side:THREE.DoubleSide,transparent:true,opacity:.78});const finVerts:number[]=[];for(let i=0;i<42;i++){const x=-length/2+.2+i*(length-.5)/41;finVerts.push(x,.22,0,x+.06,.48-(i/41)*.12,0,x+.13,.22,0)}const fgeo=new THREE.BufferGeometry();fgeo.setAttribute('position',new THREE.Float32BufferAttribute(finVerts,3));fgeo.computeVertexNormals();group.add(new THREE.Mesh(fgeo,finMat));group.rotation.y=-.12;let down=false,lx=0,ly=0,rx=.04,ry=-.12;const pd=(e:PointerEvent)=>{down=true;lx=e.clientX;ly=e.clientY};const pm=(e:PointerEvent)=>{if(!down)return;ry+=(e.clientX-lx)*.008;rx+=(e.clientY-ly)*.004;lx=e.clientX;ly=e.clientY};const pu=()=>down=false;renderer.domElement.addEventListener('pointerdown',pd);window.addEventListener('pointermove',pm);window.addEventListener('pointerup',pu);let raf=0;const loop=()=>{group.rotation.y+=(ry-group.rotation.y)*.08;group.rotation.x+=(rx-group.rotation.x)*.08;renderer.render(scene,camera);raf=requestAnimationFrame(loop)};loop();const resize=()=>{camera.aspect=el.clientWidth/el.clientHeight;camera.updateProjectionMatrix();renderer.setSize(el.clientWidth,el.clientHeight)};window.addEventListener('resize',resize);return()=>{cancelAnimationFrame(raf);window.removeEventListener('resize',resize);window.removeEventListener('pointermove',pm);window.removeEventListener('pointerup',pu);renderer.dispose();el.querySelector('canvas')?.remove()};},[]);return <div ref={ref} className="fishViewer"><div className="viewerBadge">3D MODEL</div><div className="viewerHelp">↔ ドラッグして観察</div></div>}
